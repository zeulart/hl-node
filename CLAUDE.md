## Docker

- When using docker compose, always use the latest recommended way -> "docker compose" and NOT "docker-compose"
- When dealing with containers and docker compose, NEVER EVER do compose down without services specified. The hyperliquid node MUST run at ALL TIME.